import './bootstrap';
import { initBookingForm } from './booking-form.js';

// Esegui la logica del form di prenotazione quando il DOM è pronto.
document.addEventListener('DOMContentLoaded', function() {
    initBookingForm();
});
